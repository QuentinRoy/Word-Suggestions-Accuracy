version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10.16
    steps:
      - checkout
      - run:
          name: Install deps
          command: npm ci
      - run:
          name: Run tests
          command: npm run test
